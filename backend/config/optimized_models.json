{
  "xgboost": {
    "n_estimators": 1000,
    "max_depth": 8,
    "learning_rate": 0.01,
    "subsample": 0.8,
    "colsample_bytree": 0.8,
    "random_state": 42,
    "n_jobs": -1,
    "early_stopping_rounds": 50
  },
  "cnn": {
    "sequence_length": 128,
    "filters": [
      64,
      128,
      256
    ],
    "kernel_sizes": [
      3,
      5,
      7
    ],
    "dropout_rate": 0.3,
    "batch_size": 32,
    "epochs": 100,
    "early_stopping_patience": 10
  }
}